## 1.数据库表设计

### 1.1\. 用户表 `sys_user`（普通前台学员）

|字段名|类型|约束|说明|
|---|---|---|---|
|user\_id|bigint|PK、自增|用户唯一 ID|
|username|varchar\(50\)|NOT NULL、唯一|登录账号|
|password|varchar\(100\)|NOT NULL|加密密码（BCrypt）|
|real\_name|varchar\(20\)|NULL|真实姓名|
|phone|varchar\(11\)|唯一|手机号（找回密码用）|
|email|varchar\(50\)|NULL|邮箱|
|avatar|varchar\(255\)|NULL|头像地址|
|status|tinyint|DEFAULT 1|0 禁用 1 正常|
|create\_time|datetime|DEFAULT NOW\(\)|创建时间|
|update\_time|datetime|ON UPDATE NOW\(\)|更新时间|

```sql
CREATE TABLE sys_user (
    user_id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '用户主键',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '登录账号',
    password VARCHAR(100) NOT NULL COMMENT '加密密码',
    real_name VARCHAR(20) COMMENT '真实姓名',
    phone VARCHAR(11) UNIQUE COMMENT '手机号码',
    email VARCHAR(50) COMMENT '邮箱',
    avatar VARCHAR(255) COMMENT '头像url',
    status TINYINT DEFAULT 1 COMMENT '状态0禁用1正常',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='前台学员用户表';
```

### 1.2\. 管理员表 `sys_admin`

|字段名|类型|约束|说明|
|---|---|---|---|
|admin\_id|bigint|PK 自增|管理员 ID|
|admin\_name|varchar\(50\)|唯一非空|管理员账号|
|password|varchar\(100\)|非空|加密密码|
|real\_name|varchar\(20\)||管理员姓名|
|phone|varchar\(11\)||手机号|
|email|varchar\(50\)||邮箱|
|status|tinyint|DEFAULT 1|账号状态|
|create\_time|datetime||创建时间|
|update\_time|datetime||更新时间|

```sql
CREATE TABLE sys_admin (
    admin_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    admin_name VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    real_name VARCHAR(20),
    phone VARCHAR(11),
    email VARCHAR(50),
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='后台管理员表';
```

### 1.3\. 验证码表 `verify_code`（找回密码通用）

|字段|类型|说明|
|---|---|---|
|id|bigint PK|主键|
|target|varchar\(50\)|手机号 / 邮箱|
|code|varchar\(10\)|验证码|
|type|tinyint|1 用户重置密码 2 管理员重置密码|
|expire\_time|datetime|过期时间（5 分钟）|
|create\_time|datetime|生成时间|

```sql
CREATE TABLE verify_code (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    target VARCHAR(50) NOT NULL COMMENT '手机号/邮箱',
    code VARCHAR(10) NOT NULL COMMENT '验证码',
    type TINYINT NOT NULL COMMENT '1用户 2管理员',
    expire_time DATETIME NOT NULL COMMENT '失效时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='验证码记录表';
```

### 1.4\. 培训课程表 `train_course`

|字段|类型|说明|
|---|---|---|
|course\_id|bigint PK|课程 ID|
|course\_name|varchar\(100\)|课程名称|
|course\_desc|text|课程介绍|
|cover\_img|varchar\(255\)|课程封面图|
|start\_time|datetime|开课时间|
|end\_time|datetime|结课时间|
|max\_sign|int|最大报名人数|
|status|tinyint|0 下架 1 正常展示|
|create\_admin|bigint|创建管理员 ID|
|create\_time|datetime|创建时间|

```sql
CREATE TABLE train_course (
    course_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    course_name VARCHAR(100) NOT NULL,
    course_desc TEXT,
    cover_img VARCHAR(255),
    start_time DATETIME,
    end_time DATETIME,
    max_sign INT DEFAULT 100,
    status TINYINT DEFAULT 1,
    create_admin BIGINT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='培训课程表';
```

### 1.5\. 课程报名表 `train_sign`（报名记录 \+ 审核）

|字段|类型|说明|
|---|---|---|
|sign\_id|bigint PK|报名主键|
|user\_id|bigint|报名学员 ID|
|course\_id|bigint|对应课程 ID|
|sign\_info|text|报名附加信息|
|audit\_status|tinyint|0 待审核 1 通过 2 驳回|
|audit\_admin|bigint|审核管理员 ID|
|audit\_remark|varchar\(200\)|审核备注|
|sign\_time|datetime|报名时间|
|audit\_time|datetime|审核时间|
|联合唯一索引：`user_id + course_id` 防止重复报名|||

```sql
CREATE TABLE train_sign (
    sign_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    course_id BIGINT NOT NULL,
    sign_info TEXT,
    audit_status TINYINT DEFAULT 0 COMMENT '0待审核1通过2驳回',
    audit_admin BIGINT,
    audit_remark VARCHAR(200),
    sign_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    audit_time DATETIME,
    UNIQUE KEY uk_user_course (user_id,course_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程报名表';
```

### 1.6\. 作业表 `train_homework`

|字段|类型|说明|
|---|---|---|
|homework\_id|bigint PK|作业 ID|
|course\_id|bigint|所属课程|
|homework\_title|varchar\(100\)|作业标题|
|homework\_content|text|作业要求|
|deadline|datetime|截止提交时间|
|create\_time|datetime|创建时间|

```sql
CREATE TABLE train_homework (
    homework_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    course_id BIGINT NOT NULL,
    homework_title VARCHAR(100) NOT NULL,
    homework_content TEXT,
    deadline DATETIME,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程作业表';
```

### 1.7\. 学员作业提交表 `homework_submit`

|字段|类型|说明|
|---|---|---|
|submit\_id|bigint PK|提交记录 ID|
|user\_id|bigint|提交学员|
|homework\_id|bigint|对应作业|
|submit\_content|text|文字作答内容|
|submit\_file|varchar\(500\)|附件文件地址，多个逗号分隔|
|submit\_time|datetime|提交时间|
|score|int|批阅分数（预留）|

```sql
CREATE TABLE homework_submit (
    submit_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    homework_id BIGINT NOT NULL,
    submit_content TEXT,
    submit_file VARCHAR(500),
    submit_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    score INT DEFAULT NULL,
    UNIQUE KEY uk_user_homework(user_id,homework_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学员作业提交记录表';
```

### 1.8\. 新闻公告表 `notice`

|字段|类型|说明|
|---|---|---|
|notice\_id|bigint PK|公告 ID|
|title|varchar\(100\)|公告标题|
|content|longtext|公告正文|
|cover|varchar\(255\)|公告封面|
|is\_top|tinyint|0 普通 1 置顶|
|status|tinyint|0 下架 1 正常前台展示|
|create\_admin|bigint|发布管理员 ID|
|create\_time|datetime|发布时间|
|update\_time|datetime|修改时间|

```sql
CREATE TABLE notice (
    notice_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(100) NOT NULL,
    content LONGTEXT NOT NULL,
    cover VARCHAR(255),
    is_top TINYINT DEFAULT 0,
    status TINYINT DEFAULT 1,
    create_admin BIGINT NOT NULL,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='官网新闻公告表';
```

### 1.9\. 报名总开关配置表 `system_config`

全局控制整个系统是否允许报名

```sql
CREATE TABLE system_config (
    config_id INT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(50) UNIQUE NOT NULL COMMENT '配置键',
    config_value VARCHAR(100) NOT NULL COMMENT '配置值',
    remark VARCHAR(200)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
-- 初始化数据：报名开关 1开启 0关闭
INSERT INTO system_config(config_key,config_value,remark) VALUES('train_sign_switch','1','全局培训报名总开关');
```

---

# 2.统一接口规范

## 2.1\. 请求前缀

- 前台用户接口：`/api/v1/user`

- 管理员后台接口：`/api/v1/admin`

## 2.2\. 统一返回格式

```json
{
  "code": 200, // 200成功 500服务异常 401未登录 403无权限
  "msg": "操作提示文字",
  "data": {} // 业务数据
}
```

## 2.3\. 鉴权规则

所有需要登录的接口，请求头携带：`Authorization: Bearer {accessToken}`
刷新 Token 接口使用 refreshToken 换取新 accessToken

## 2.4\. 请求方式约定

- GET：查询数据

- POST：新增、提交、登录、批量操作

- PUT：全量更新、状态修改

- DELETE：删除数据

# 3.前台用户端接口文档--lx

## 3.1.认证管理 /api/v1/user/auth

|接口名称|请求方式|接口地址|请求参数|返回 data|鉴权|
|---|---|---|---|---|---|
|用户登录|POST|/login|body: username,password|accessToken,refreshToken,userInfo|无需|
|用户登出|POST|/logout|header:token|null|需要|
|获取当前用户信息|GET|/info|header:token|用户基础资料（姓名、头像、手机号）|需要|
|修改个人资料|POST|/update\_info|body:realName,avatar,email|更新后的用户信息|需要|
|修改密码|POST|/update\_pwd|body:oldPwd,newPwd|null|需要|
|刷新 Access Token|POST|/refresh\_token|body:refreshToken|新 accessToken|无需|

### 3.1.1. 用户登录

接口地址：`POST /api/v1/user/auth/login` 

功能描述：账号密码登录，下发accessToken、refreshToken与用户基础信息 

请求参数（Body-JSON）

| 参数名   | 类型   | 必填 | 说明                       |
| -------- | ------ | ---- | -------------------------- |
| username | string | 是   | 登录账号                   |
| password | string | 是   | 登录明文密码，后端加密校验 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userInfo": {
      "user_id": 1001,
      "username": "stu001",
      "real_name": "张三",
      "avatar": "shturl.cc/NJYIRAMtVG8a35wSeivhg",
      "phone": "13800138000"
    }
  }
}
```

### 3.1.2. 用户登出

接口地址：`POST /api/v1/user/auth/logout` 

功能描述：销毁当前登录Token，清除登录状态 

请求头：携带登录Token 

请求参数：无请求参数 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "登出成功",
  "data": null
}
```

### 3.1.3. 获取当前用户信息

接口地址：`GET /api/v1/user/auth/info` 

功能描述：根据Token查询登录用户个人资料 

请求头：携带登录Token 

请求参数：无 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "user_id": 1001,
    "username": "stu001",
    "real_name": "张三",
    "avatar": "shturl.cc/NJYIRAMtVG8a35wSeivhg",
    "phone": "13800138000",
    "email": "zhangsan@shturl."
  }
}
```

### 3.1.4. 修改个人资料

接口地址：`POST /api/v1/user/auth/update_info` 

功能描述：修改用户昵称、头像、邮箱等基础资料 

请求头：携带登录Token 

请求参数（Body-JSON）

| 参数名   | 类型   | 必填 | 说明             |
| -------- | ------ | ---- | ---------------- |
| realName | string | 否   | 真实姓名         |
| avatar   | string | 否   | 头像文件访问地址 |
| email    | string | 否   | 绑定邮箱         |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "资料修改成功",
  "data": {
    "user_id": 1001,
    "real_name": "张三三",
    "avatar": "https://shturl./newavatar.png",
    "email": "new@shturl."
  }
}
```

### 3.1.5. 修改密码

接口地址：`POST /api/v1/user/auth/update_pwd` 

功能描述：登录状态下，输入旧密码更换新密码 

请求头：携带登录Token 

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| oldPwd | string | 是   | 原始登录密码 |
| newPwd | string | 是   | 新密码       |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "密码修改成功，请重新登录",
  "data": null
}
```

### 3.1.6. 刷新Access Token

接口地址：`POST /api/v1/user/auth/refresh_token` 

功能描述：accessToken过期后，使用refreshToken换取全新短期令牌 

无需登录鉴权 

请求参数（Body-JSON）

| 参数名       | 类型   | 必填 | 说明                 |
| ------------ | ------ | ---- | -------------------- |
| refreshToken | string | 是   | 登录时下发的刷新令牌 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "刷新成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

## 3.2忘记密码 /api/user/pwd\_reset

|接口名称|请求方式|地址|请求参数|返回|
|---|---|---|---|---|
|发送重置密码验证码|POST|/send\_code|body:phone|发送结果提示|
|重置密码|POST|/reset\_pwd|body:phone,code,newPwd|重置成功|

### 3.2.1. 发送重置密码验证码

接口地址：`POST /api/v1/user/pwd_reset/send_code` 

功能描述：向用户手机号发送短信验证码，用于找回密码 

无需登录鉴权 

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明           |
| ------ | ------ | ---- | -------------- |
| phone  | string | 是   | 绑定的手机号码 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "验证码已发送，5分钟内有效",
  "data": null
}
```

### 3.2.2. 重置密码

接口地址：`POST /api/v1/user/pwd_reset/reset_pwd` 

功能描述：填写手机号+验证码，重置账号登录密码 

无需登录鉴权 

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明             |
| ------ | ------ | ---- | ---------------- |
| phone  | string | 是   | 手机号           |
| code   | string | 是   | 收到的短信验证码 |
| newPwd | string | 是   | 重置后的新密码   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "密码重置成功，请使用新密码登录",
  "data": null
}
```

## 3.3.用户端培训 /api/user/train

|接口名称|请求方式|地址|参数|返回 data|鉴权|
|---|---|---|---|---|---|
|获取我的培训课程列表|GET|/course/list|分页参数 page,size|课程分页列表|需要|
|报名培训课程|POST|/course/sign|body:courseId,signInfo|报名记录 ID、待审核状态|需要|
|我的报名记录|GET|/sign/list|page,size|本人所有报名记录 \+ 审核状态|需要|
|我的作业列表|GET|/homework/list|courseId \(可选\)|对应课程作业集合|需要|
|提交作业|POST|/homework/submit|body:homeworkId,content,file \(文件表单\)|提交记录 ID|需要|

### 3.3.1. 获取我的培训课程列表

接口地址：`GET /api/V1/user/train/course/list?page=1&size=10` 

功能描述：分页查询实验室对外开放的全部培训课程 

请求头：携带登录Token 

请求参数（Query参数）

| 参数名 | 类型 | 必填 | 说明             |
| ------ | ---- | ---- | ---------------- |
| page   | int  | 否   | 页码，默认1      |
| size   | int  | 否   | 每页条数，默认10 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 8,
    "list": [
      {
        "course_id": 12,
        "course_name": "嵌入式系统实训",
        "course_desc": "讲解单片机硬件开发基础",
        "cover_img": "https://shturl./course12.jpg",
        "start_time": "2026-08-01 09:00:00",
        "end_time": "2026-09-30 17:00:00",
        "max_sign": 50
      }
    ]
  }
}
```

### 3.3.2. 报名培训课程

接口地址：`POST /api/V1/user/train/course/sign` 

功能描述：用户提交对应课程的报名申请，状态默认为待审核 

请求头：携带登录Token 

请求参数（Body-JSON）

| 参数名   | 类型   | 必填 | 说明                   |
| -------- | ------ | ---- | ---------------------- |
| courseId | bigint | 是   | 目标课程ID             |
| signInfo | string | 否   | 报名备注、个人申请说明 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "报名提交成功，等待管理员审核",
  "data": {
    "sign_id": 20260724001,
    "audit_status": 0,
    "status_text": "待审核"
  }
}
```

### 3.3.3. 我的报名记录

接口地址：`GET /api/v1/user/train/sign/list?page=1&size=10` 

功能描述：分页查看本人所有课程报名记录与审核结果 

请求头：携带登录Token 

请求参数（Query参数）

| 参数名 | 类型 | 必填 | 说明     |
| ------ | ---- | ---- | -------- |
| page   | int  | 否   | 页码     |
| size   | int  | 否   | 每页条数 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 2,
    "list": [
      {
        "sign_id": 20260724001,
        "course_id": 12,
        "course_name": "嵌入式系统实训",
        "sign_info": "本人有C语言基础",
        "audit_status": 1,
        "status_text": "审核通过",
        "audit_remark": "审核通过，按时上课",
        "sign_time": "2026-07-24 10:20:33",
        "audit_time": "2026-07-24 11:05:12"
      }
    ]
  }
}
```

### 3.3.4. 我的作业列表

接口地址：`GET /api/v1/user/train/homework/list?page=1&size=10&courseId=12` 

功能描述：查看已通过报名课程对应的所有作业任务 

请求头：携带登录Token 

请求参数（Query参数）

| 参数名   | 类型   | 必填 | 说明             |
| -------- | ------ | ---- | ---------------- |
| page     | int    | 否   | 页码             |
| size     | int    | 否   | 每页条数         |
| courseId | bigint | 否   | 筛选指定课程作业 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 3,
    "list": [
      {
        "homework_id": 56,
        "course_id": 12,
        "homework_title": "第一章代码作业",
        "homework_content": "完成LED点亮代码编写",
        "deadline": "2026-08-10 23:59:59",
        "create_time": "2026-08-01 14:30:00"
      }
    ]
  }
}
```

### 3.3.5. 提交作业

接口地址：`POST /api/v1/user/train/homework/submit` 

功能描述：提交作业文字内容与附件文件 

请求头：携带登录Token 

请求参数（Form表单格式，含文件上传）

| 参数名     | 类型   | 必填 | 说明                     |
| ---------- | ------ | ---- | ------------------------ |
| homeworkId | bigint | 是   | 作业ID                   |
| content    | string | 否   | 文字作答内容             |
| file       | file   | 否   | 作业附件文件，支持多文件 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "作业提交成功",
  "data": {
    "submit_id": 897,
    "submit_time": "2026-07-24 15:30:22"
  }
}
```

# 4.管理员后台接口文档--wjc

## 4.1.管理员认证 /api/v1/admin/auth

|接口|Method|地址|参数|返回||
|---|---|---|---|---|---|
|管理员登录|POST|/login|adminName,password|accessToken、管理员信息|无鉴权|
|管理员登出|POST|/logout|token|成功提示|需鉴权|
|获取管理员信息|GET|/info|token|管理员资料|需鉴权|
|修改密码|POST|/update\_pwd|oldPwd、newPwd|成功|需鉴权|
|发送验证码|POST|/send\_code|phone|发送结果|无鉴权|
|重置密码|POST|/reset\_pwd|phone、code、newPwd|重置完成|无鉴权|

### 4.1.1. 管理员登录

接口地址：`POST /api/v1/admin/auth/login` 

功能描述：后台管理员账号登录，下发登录令牌 

无需鉴权 

请求参数（Body-JSON）

| 参数名    | 类型   | 必填 | 说明       |
| --------- | ------ | ---- | ---------- |
| adminName | string | 是   | 管理员账号 |
| password  | string | 是   | 登录密码   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "adminInfo": {
      "admin_id": 1,
      "admin_name": "admin",
      "real_name": "系统管理员",
      "phone": "13900139000"
    }
  }
}
```

### 4.1.2. 管理员登出

接口地址：`POST /api/v1/admin/auth/logout` 

功能描述：销毁管理员登录凭证 

请求头：管理员Token 

请求参数：无 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "后台登出成功",
  "data": null
}
```

### 4.1.3. 获取管理员信息

接口地址：`GET /api/v1/admin/auth/info` 
功能描述：获取当前登录管理员个人信息 

请求头：管理员Token 

请求参数：无 
响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "admin_id": 1,
    "admin_name": "admin",
    "real_name": "系统管理员",
    "phone": "13900139000",
    "email": "admin@lab.com"
  }
}
```

### 4.1.4. 修改密码

接口地址：`POST /api/v1/admin/auth/update_pwd` 

功能描述：管理员修改自身登录密码 

请求头：管理员Token 

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明   |
| ------ | ------ | ---- | ------ |
| oldPwd | string | 是   | 旧密码 |
| newPwd | string | 是   | 新密码 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "管理员密码修改完成",
  "data": null
}
```

### 4.1.5. 发送验证码（管理员找回密码）

接口地址：`POST /api/v1/admin/auth/send_code` 

无需鉴权 

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明             |
| ------ | ------ | ---- | ---------------- |
| phone  | string | 是   | 管理员绑定手机号 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "验证码已下发",
  "data": null
}
```

### 4.1.6. 管理员重置密码

接口地址：`POST /api/v1/admin/auth/reset_pwd` 

无需鉴权 

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明       |
| ------ | ------ | ---- | ---------- |
| phone  | string | 是   | 手机号     |
| code   | string | 是   | 短信验证码 |
| newPwd | string | 是   | 新登录密码 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "管理员密码重置成功",
  "data": null
}
```

## 4.2.新闻公告 /api/v1/admin/notice

|接口|Method|地址|参数||
|---|---|---|---|---|
|创建公告|POST|/create|title、content、cover、isTop||
|删除公告|DELETE|/delete/\{noticeId\}|路径参数 id||
|更新公告|PUT|/update/\{noticeId\}|公告全部字段||
|获取公告列表|GET|/list|page、size、title 关键词|后台管理分页列表（包含已下架数据）|
|获取公告详情|GET|/detail/\{id\}|公告 id|完整公告内容|
|前台获取公告列表|GET|/front/list|page、size|只返回 status=1 正常、可展示公告|
|前台获取公告详情|GET|/front/detail/\{id\}|id|公开公告详情|

### 4.2.1. 创建公告

接口地址：`POST /api/v1/admin/notice/create` 

请求头：管理员Token 

请求参数（Body-JSON）

| 参数名  | 类型   | 必填 | 说明                       |
| ------- | ------ | ---- | -------------------------- |
| title   | string | 是   | 公告标题                   |
| content | string | 是   | 公告正文内容               |
| cover   | string | 否   | 公告封面图片地址           |
| isTop   | int    | 否   | 0普通公告 1置顶公告，默认0 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "公告发布成功",
  "data": {
    "notice_id": 105,
    "title": "实验室暑期培训安排通知"
  }
}
```

### 4.2.2. 删除公告

接口地址：`DELETE /api/v1/admin/notice/delete/{noticeId}` 

请求头：管理员Token 

请求参数（路径参数）

| 参数名   | 类型   | 必填 | 说明         |
| -------- | ------ | ---- | ------------ |
| noticeId | bigint | 是   | 待删除公告ID |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "公告删除成功",
  "data": null
}
```

### 4.2.3. 更新公告

接口地址：`PUT /api/v1/admin/notice/update/{noticeId}` 

请求头：管理员Token 

路径参数：noticeId（公告ID） Body入参

| 参数名  | 类型   | 必填 | 说明        |
| ------- | ------ | ---- | ----------- |
| title   | string | 否   | 公告标题    |
| content | string | 否   | 公告正文    |
| cover   | string | 否   | 封面图      |
| isTop   | int    | 否   | 是否置顶    |
| status  | int    | 否   | 0下架 1展示 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "公告编辑成功",
  "data": {
    "notice_id": 105,
    "title": "实验室暑期培训安排通知（修订版）"
  }
}
```

### 4.2.4. 后台获取公告列表

接口地址：`GET /api/v1/admin/notice/list?page=1&size=10&title=` 

请求头：管理员Token Query参数

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| page   | int    | 否   | 页码         |
| size   | int    | 否   | 每页条数     |
| title  | string | 否   | 标题模糊搜索 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 12,
    "list": [
      {
        "notice_id": 105,
        "title": "实验室暑期培训安排通知",
        "is_top": 1,
        "status": 1,
        "create_admin": 1,
        "create_time": "2026-07-20 09:10:00"
      }
    ]
  }
}
```

### 4.2.5. 获取公告详情（后台）

接口地址：`GET /api/v1/admin/notice/detail/{noticeId}` 

请求头：管理员Token 

路径参数：noticeId 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "notice_id": 105,
    "title": "实验室暑期培训安排通知",
    "content": "详细培训时间、地点、报名要求......",
    "cover": "https://shturl./notice105.jpg",
    "is_top": 1,
    "status": 1,
    "create_time": "2026-07-20 09:10:00"
  }
}
```

### 4.2.6. 前台获取公告列表

接口地址：`GET /api/v1/admin/notice/front/list?page=1&size=10` 

无需管理员登录，官网首页调用 

响应成功示例（只返回上架公告）

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 8,
    "list": [
      {
        "notice_id": 105,
        "title": "实验室暑期培训安排通知",
        "cover": "https://shturl./notice105.jpg",
        "is_top": 1,
        "create_time": "2026-07-20 09:10:00"
      }
    ]
  }
}
```

### 4.2.7. 前台获取公告详情

接口地址：`GET /api/v1/admin/notice/front/detail/{noticeId}` 

无需鉴权，官网用户查看公告详情 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "notice_id": 105,
    "title": "实验室暑期培训安排通知",
    "content": "详细培训时间、地点、报名要求......",
    "cover": "https://shturl./notice105.jpg",
    "create_time": "2026-07-20 09:10:00"
  }
}
```

## 4.3.报名开关配置 /api/v1/admin/sign\_switch

|接口|Method|地址|参数||
|---|---|---|---|---|
|查询报名开关|GET|/get|无|返回 0 关闭 / 1 开启|
|修改报名开关|PUT|/update|body:\{value:0/1\}|修改状态|
|前台查询报名开关|GET|/front/get|无|给官网首页展示是否开放报名|

### 4.3.1. 查询报名开关

接口地址：`GET /api/v1/admin/sign_switch/get` 

请求头：管理员Token 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "value": 1,
    "text": "报名功能已开启"
  }
}
```

### 4.3.2. 修改报名开关

接口地址：`PUT /api/v1/admin/sign_switch/update` 

请求头：管理员Token 

Body参数

| 参数名 | 类型 | 必填 | 说明                |
| ------ | ---- | ---- | ------------------- |
| value  | int  | 是   | 0关闭报名 1开启报名 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "报名状态修改成功",
  "data": {
    "value": 0,
    "text": "报名功能已关闭"
  }
}
```

### 4.3.3. 前台查询报名开关

接口地址：`GET /api/v1/admin/sign_switch/front/get` 

无需鉴权，官网首页展示报名状态 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "value": 1,
    "tips": "当前培训报名开放中"
  }
}
```

## 4.4.报名审核 /api/v1/admin/sign\_audit

|接口|Method|地址|参数||
|---|---|---|---|---|
|获取报名列表|GET|/list|page、size、auditStatus、courseId 筛选|所有用户报名数据分页|
|获取报名详情|GET|/detail/\{signId\}|报名 ID|报名人信息 \+ 报名内容|
|单条审核报名|PUT|/single\_audit/\{signId\}|body:\{auditStatus,auditRemark\}|审核结果|
|批量审核|POST|/batch\_audit|body:\{signId 数组，auditStatus,auditRemark\}|批量处理成功数量|

### 4.4.1. 获取报名列表

接口地址：`GET /api/v1/admin/sign_audit/list?page=1&size=10&auditStatus=0&courseId=` 

请求头：管理员Token 

Query参数

| 参数名      | 类型   | 必填 | 说明                            |
| ----------- | ------ | ---- | ------------------------------- |
| page        | int    | 否   | 页码                            |
| size        | int    | 否   | 每页条数                        |
| auditStatus | int    | 否   | 筛选审核状态0待审核/1通过/2驳回 |
| courseId    | bigint | 否   | 筛选指定课程报名数据            |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 25,
    "list": [
      {
        "sign_id": 20260724001,
        "user_id": 1001,
        "real_name": "张三",
        "course_id": 12,
        "course_name": "嵌入式系统实训",
        "sign_info": "本人有C语言基础",
        "audit_status": 0,
        "status_text": "待审核",
        "sign_time": "2026-07-24 10:20:33"
      }
    ]
  }
}
```

### 4.4.2. 获取报名详情

接口地址：`GET /api/v1/admin/sign_audit/detail/{signId}` 

请求头：管理员Token 

路径参数：signId（报名记录ID） 

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "sign_id": 20260724001,
    "user_real_name": "张三",
    "user_phone": "13800138000",
    "course_name": "嵌入式系统实训",
    "sign_info": "本人有C语言基础，希望参与本次实训",
    "audit_status": 0,
    "audit_remark": null,
    "sign_time": "2026-07-24 10:20:33"
  }
}
```

### 4.4.3. 单条审核报名

接口地址：`PUT /api/v1/admin/sign_audit/single_audit/{signId}` 

请求头：管理员Token 

路径参数：signId 

Body参数

| 参数名      | 类型   | 必填 | 说明         |
| ----------- | ------ | ---- | ------------ |
| auditStatus | int    | 是   | 1通过 2驳回  |
| auditRemark | string | 否   | 审核备注说明 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "单条审核操作完成",
  "data": {
    "sign_id": 20260724001,
    "audit_status": 1,
    "status_text": "审核通过"
  }
}
```

### 4.4.4. 批量审核报名

接口地址：`POST /api/v1/admin/sign_audit/batch_audit` 

请求头：管理员Token 

Body参数

| 参数名      | 类型   | 必填 | 说明           |
| ----------- | ------ | ---- | -------------- |
| signIdList  | array  | 是   | 报名ID数组集合 |
| auditStatus | int    | 是   | 1通过 / 2驳回  |
| auditRemark | string | 否   | 统一审核备注   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "批量审核完成",
  "data": {
    "successCount": 8
  }
}
```