## 1.数据库表设计

### 1.1. 用户表 `sys_user`（普通前台学员）

|字段名|类型|约束|说明|
|---|---|---|---|
|user\_id|bigint|PK、自增|用户唯一 ID|
|username|varchar\(50\)|NOT NULL、唯一|登录账号|
|password|varchar\(100\)|NOT NULL|加密密码（BCrypt）|
|real\_name|varchar\(20\)|NULL|真实姓名|
|phone|varchar\(11\)|唯一|手机号|
|email|varchar\(50\)|NULL|邮箱|
|avatar|varchar\(255\)|NULL|头像地址|
|grade|varchar\(20\)|NULL|年级|
|major|varchar\(50\)|NULL|专业|
|college|varchar\(50\)|NULL|学院|
|student\_id|varchar\(20\)|NULL|学号|
|status|tinyint|DEFAULT 1|0 禁用 1 正常|
|create\_time|datetime|DEFAULT NOW\(\)|创建时间|
|update\_time|datetime|ON UPDATE NOW\(\)|更新时间|

```sql
CREATE TABLE sys_user (
    user_id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '用户主键',
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '登录账号',
    password VARCHAR(100) NOT NULL COMMENT '加密密码',
    real_name VARCHAR(20) COMMENT '真实姓名',
    phone VARCHAR(11) UNIQUE COMMENT '手机号码',
    email VARCHAR(50) COMMENT '邮箱',
    avatar VARCHAR(255) COMMENT '头像url',
    grade VARCHAR(20) COMMENT '年级',
    major VARCHAR(50) COMMENT '专业',
    college VARCHAR(50) COMMENT '学院',
    student_id VARCHAR(20) COMMENT '学号',
    status TINYINT DEFAULT 1 COMMENT '状态0禁用1正常',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='前台学员用户表';
```

### 1.2. 管理员表 `sys_admin`

|字段名|类型|约束|说明|
|---|---|---|---|
|admin\_id|bigint|PK 自增|管理员 ID|
|admin\_name|varchar\(50\)|唯一非空|管理员账号|
|password|varchar\(100\)|非空|加密密码|
|real\_name|varchar\(20\)||管理员姓名|
|phone|varchar\(11\)||手机号|
|email|varchar\(50\)||邮箱|
|status|tinyint|DEFAULT 1|账号状态|
|create\_time|datetime||创建时间|
|update\_time|datetime||更新时间|

```sql
CREATE TABLE sys_admin (
    admin_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    admin_name VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(100) NOT NULL,
    real_name VARCHAR(20),
    phone VARCHAR(11),
    email VARCHAR(50),
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='后台管理员表';
```

### 1.3. 验证码表 `verify_code`（找回密码通用）

|字段|类型|说明|
|---|---|---|
|id|bigint PK|主键|
|target|varchar\(50\)|手机号 / 邮箱|
|code|varchar\(10\)|验证码|
|type|tinyint|1 用户重置密码 2 管理员重置密码 3 注册 4 管理员重置密码(邮箱)|
|expire\_time|datetime|过期时间（5 分钟）|
|create\_time|datetime|生成时间|

```sql
CREATE TABLE verify_code (
    id BIGINT AUTO_INCREMENT PRIMARY KEY,
    target VARCHAR(50) NOT NULL COMMENT '手机号/邮箱',
    code VARCHAR(10) NOT NULL COMMENT '验证码',
    type TINYINT NOT NULL COMMENT '1用户 2管理员',
    expire_time DATETIME NOT NULL COMMENT '失效时间',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='验证码记录表';
```

### 1.4. 培训课程表 `train_course`

|字段|类型|说明|
|---|---|---|
|course\_id|bigint PK|课程 ID|
|course\_name|varchar\(100\)|课程名称|
|course\_desc|text|课程介绍|
|cover\_img|varchar\(255\)|课程封面图|
|start\_time|datetime|开课时间|
|end\_time|datetime|结课时间|
|max\_sign|int|最大报名人数|
|status|tinyint|0 下架 1 正常展示|
|create\_admin|bigint|创建管理员 ID|
|create\_time|datetime|创建时间|

```sql
CREATE TABLE train_course (
    course_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    course_name VARCHAR(100) NOT NULL,
    course_desc TEXT,
    cover_img VARCHAR(255),
    start_time DATETIME,
    end_time DATETIME,
    max_sign INT DEFAULT 100,
    status TINYINT DEFAULT 1,
    create_admin BIGINT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='培训课程表';
```

### 1.5. 课程报名表 `train_sign`

|字段|类型|说明|
|---|---|---|
|sign\_id|bigint PK|报名主键|
|user\_id|bigint|报名学员 ID|
|course\_id|bigint|对应课程 ID|
|sign\_info|text|报名附加信息|
|status|tinyint|DEFAULT 1|1 正常 0 取消|
|sign\_time|datetime|报名时间|
|联合唯一索引：`user_id + course_id` 防止重复报名|||

```sql
CREATE TABLE train_sign (
    sign_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    course_id BIGINT NOT NULL,
    sign_info TEXT,
    status TINYINT DEFAULT 1 COMMENT '1正常 0取消',
    sign_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_course (user_id,course_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程报名表';
```

### 1.6. 作业表 `train_homework`

|字段|类型|说明|
|---|---|---|
|homework\_id|bigint PK|作业 ID|
|course\_id|bigint|所属课程|
|homework\_title|varchar\(100\)|作业标题|
|homework\_content|text|作业要求|
|deadline|datetime|截止提交时间|
|create\_admin|bigint|布置作业的管理员 ID|
|create\_time|datetime|创建时间|

```sql
CREATE TABLE train_homework (
    homework_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    course_id BIGINT NOT NULL,
    homework_title VARCHAR(100) NOT NULL,
    homework_content TEXT,
    deadline DATETIME,
    create_admin BIGINT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='课程作业表';
```

### 1.7. 学员作业提交表 `homework_submit`

|字段|类型|说明|
|---|---|---|
|submit\_id|bigint PK|提交记录 ID|
|user\_id|bigint|提交学员|
|homework\_id|bigint|对应作业|
|submit\_content|text|文字作答内容|
|submit\_file|varchar\(500\)|附件文件地址，多个逗号分隔|
|submit\_time|datetime|提交时间|
|score|int|批阅分数|
|remark|varchar\(200\)|教师评语|

```sql
CREATE TABLE homework_submit (
    submit_id BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id BIGINT NOT NULL,
    homework_id BIGINT NOT NULL,
    submit_content TEXT,
    submit_file VARCHAR(500),
    submit_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    score INT DEFAULT NULL,
    remark VARCHAR(200) DEFAULT NULL,
    UNIQUE KEY uk_user_homework(user_id,homework_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学员作业提交记录表';
```

### 1.8. 报名总开关配置表 `system_config`

全局控制整个系统是否允许报名

```sql
CREATE TABLE system_config (
    config_id INT PRIMARY KEY AUTO_INCREMENT,
    config_key VARCHAR(50) UNIQUE NOT NULL COMMENT '配置键',
    config_value VARCHAR(100) NOT NULL COMMENT '配置值',
    remark VARCHAR(200)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
-- 初始化数据：报名开关 1开启 0关闭
INSERT INTO system_config(config_key,config_value,remark) VALUES('train_sign_switch','1','全局培训报名总开关');
```

---

# 2.统一接口规范

## 2.1. 请求前缀

- 前台用户接口：`/api/v1/user`

- 管理员后台接口：`/api/v1/admin`

## 2.2. 统一返回格式

```json
{
  "code": 200, // 200成功 500服务异常 401未登录 403无权限
  "msg": "操作提示文字",
  "data": {} // 业务数据
}
```

## 2.3. 鉴权规则

所有需要登录的接口，请求头携带：`Authorization: Bearer {accessToken}`
刷新 Token 接口使用 refreshToken 换取新 accessToken

## 2.4. 请求方式约定

- GET：查询数据

- POST：新增、提交、登录、批量操作

- PUT：全量更新、状态修改

- DELETE：删除数据

# 3.前台用户端接口文档--lx

## 3.1.认证管理 /api/v1/user/auth

|接口名称|请求方式|接口地址|请求参数|返回 data|鉴权|
|---|---|---|---|---|---|
|用户登录|POST|/login|body: username,password|accessToken,refreshToken,userInfo|无需|
|用户登出|POST|/logout|header:token|null|需要|
|获取当前用户信息|GET|/info|header:token|用户基础资料|需要|
|修改个人资料|POST|/update\_info|body:realName,avatar,email,phone,grade,major,college|更新后的用户信息|需要|
|修改密码|POST|/update\_pwd|body:oldPwd,newPwd|null|需要|
|刷新 Access Token|POST|/refresh\_token|body:refreshToken|新 accessToken|无需|

### 3.1.1. 用户登录

接口地址：`POST /api/v1/user/auth/login`

功能描述：账号密码登录，下发accessToken、refreshToken与用户基础信息

请求参数（Body-JSON）

| 参数名   | 类型   | 必填 | 说明                       |
| -------- | ------ | ---- | -------------------------- |
| username | string | 是   | 登录账号                   |
| password | string | 是   | 登录明文密码，后端加密校验 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "refreshToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userInfo": {
      "user_id": 1001,
      "username": "stu001",
      "real_name": "张三",
      "avatar": "shturl.cc/NJYIRAMtVG8a35wSeivhg",
      "phone": "13800138000",
      "email": "zhangsan@test.com",
      "grade": "2023级",
      "major": "计算机科学",
      "college": "计算机与软件学院",
      "student_id": "202301001"
    }
  }
}
```

### 3.1.2. 用户登出

接口地址：`POST /api/v1/user/auth/logout`

功能描述：销毁当前登录Token，清除登录状态

请求头：携带登录Token

请求参数：无请求参数

响应成功示例

```JSON
{
  "code": 200,
  "msg": "登出成功",
  "data": null
}
```

### 3.1.3. 获取当前用户信息

接口地址：`GET /api/v1/user/auth/info`

功能描述：根据Token查询登录用户个人资料

请求头：携带登录Token

请求参数：无

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "user_id": 1001,
    "username": "stu001",
    "real_name": "张三",
    "avatar": "shturl.cc/NJYIRAMtVG8a35wSeivhg",
    "phone": "13800138000",
    "email": "zhangsan@test.com",
    "grade": "2023级",
    "major": "计算机科学",
    "college": "计算机与软件学院",
    "student_id": "202301001"
  }
}
```

### 3.1.4. 修改个人资料

接口地址：`POST /api/v1/user/auth/update_info`

功能描述：修改用户昵称、头像、邮箱等基础资料

请求头：携带登录Token

请求参数（Body-JSON）

| 参数名   | 类型   | 必填 | 说明             |
| -------- | ------ | ---- | ---------------- |
| realName | string | 否   | 真实姓名         |
| avatar   | string | 否   | 头像文件访问地址 |
| email    | string | 否   | 绑定邮箱         |
| phone    | string | 否   | 手机号           |
| grade    | string | 否   | 年级             |
| major    | string | 否   | 专业             |
| college  | string | 否   | 学院             |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "资料修改成功",
  "data": {
    "user_id": 1001,
    "real_name": "张三三",
    "avatar": "https://shturl./newavatar.png",
    "email": "new@shturl."
  }
}
```

### 3.1.5. 修改密码

接口地址：`POST /api/v1/user/auth/update_pwd`

功能描述：登录状态下，输入旧密码更换新密码

请求头：携带登录Token

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| oldPwd | string | 是   | 原始登录密码 |
| newPwd | string | 是   | 新密码       |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "密码修改成功，请重新登录",
  "data": null
}
```

### 3.1.6. 刷新Access Token

接口地址：`POST /api/v1/user/auth/refresh_token`

功能描述：accessToken过期后，使用refreshToken换取全新短期令牌

无需登录鉴权

请求参数（Body-JSON）

| 参数名       | 类型   | 必填 | 说明                 |
| ------------ | ------ | ---- | -------------------- |
| refreshToken | string | 是   | 登录时下发的刷新令牌 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "刷新成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

## 3.2.忘记密码 /api/v1/user/pwd_reset

|接口名称|请求方式|地址|请求参数|返回|
|---|---|---|---|---|
|发送重置密码验证码|POST|/send\_code|body:email|发送结果提示|
|重置密码|POST|/reset\_pwd|body:email,code,newPwd|重置成功|

### 3.2.1. 发送重置密码验证码

接口地址：`POST /api/v1/user/pwd_reset/send_code`

功能描述：向用户邮箱发送验证码，用于找回密码

无需登录鉴权

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明         |
| ------ | ------ | ---- | ------------ |
| email  | string | 是   | 绑定邮箱     |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "验证码已发送，5分钟内有效",
  "data": null
}
```

### 3.2.2. 重置密码

接口地址：`POST /api/v1/user/pwd_reset/reset_pwd`

功能描述：填写邮箱+验证码，重置账号登录密码

无需登录鉴权

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明             |
| ------ | ------ | ---- | ---------------- |
| email  | string | 是   | 邮箱             |
| code   | string | 是   | 收到的邮件验证码 |
| newPwd | string | 是   | 重置后的新密码   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "密码重置成功，请使用新密码登录",
  "data": null
}
```

## 3.3.课程查看与报名 /api/v1/user/course

|接口名称|请求方式|地址|参数|返回 data|鉴权|
|---|---|---|---|---|---|
|课程列表|GET|/list|分页参数 page,size|上架课程分页列表|需要|
|课程详情|GET|/detail/{courseId}|路径参数|课程完整信息|需要|
|报名课程|POST|/sign|body:courseId,signInfo|报名记录 ID、待审核状态|需要|
|我的报名记录|GET|/my\_sign|page,size|本人所有报名记录 + 审核状态|需要|

### 3.3.1. 课程列表

接口地址：`GET /api/v1/user/course/list?page=1&size=10`

功能描述：分页查询所有上架培训课程

请求头：携带登录Token

请求参数（Query参数）

| 参数名 | 类型 | 必填 | 说明             |
| ------ | ---- | ---- | ---------------- |
| page   | int  | 否   | 页码，默认1      |
| size   | int  | 否   | 每页条数，默认10 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 5,
    "list": [
      {
        "course_id": 1,
        "course_name": "嵌入式系统实训",
        "course_desc": "讲解单片机硬件开发基础",
        "cover_img": "https://xxx/course1.jpg",
        "start_time": "2026-08-01 09:00:00",
        "end_time": "2026-09-30 17:00:00",
        "max_sign": 50,
        "signed_count": 25
      }
    ]
  }
}
```

### 3.3.2. 课程详情

接口地址：`GET /api/v1/user/course/detail/{courseId}`

功能描述：查看课程完整信息

请求头：携带登录Token

### 3.3.3. 报名课程

接口地址：`POST /api/v1/user/course/sign`

功能描述：用户直接报名课程，无需审核

请求头：携带登录Token

请求参数（Body-JSON）

| 参数名   | 类型   | 必填 | 说明                   |
| -------- | ------ | ---- | ---------------------- |
| courseId | bigint | 是   | 目标课程ID             |
| signInfo | string | 否   | 报名备注、个人申请说明 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "报名成功",
  "data": {
    "sign_id": 1,
    "course_name": "嵌入式系统实训",
    "sign_time": "2026-07-24 10:20:33"
  }
}
```

### 3.3.4. 我的报名记录

接口地址：`GET /api/v1/user/course/my_sign?page=1&size=10`

功能描述：分页查看本人所有课程报名记录

请求头：携带登录Token

请求参数（Query参数）

| 参数名 | 类型 | 必填 | 说明     |
| ------ | ---- | ---- | -------- |
| page   | int  | 否   | 页码     |
| size   | int  | 否   | 每页条数 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 2,
    "list": [
      {
        "sign_id": 1,
        "course_id": 1,
        "course_name": "嵌入式系统实训",
        "sign_info": "本人有C语言基础",
        "sign_time": "2026-07-24 10:20:33"
      }
    ]
  }
}
```

## 3.4.作业 /api/v1/user/homework

|接口名称|请求方式|地址|参数|返回 data|鉴权|
|---|---|---|---|---|---|
|作业列表|GET|/list|page,size,courseId|已报名课程的作业|需要|
|作业详情|GET|/detail/{homeworkId}|路径参数|作业完整信息|需要|
|提交作业|POST|/submit|body:homeworkId,content + file|提交记录 ID|需要|
|我的提交记录|GET|/my\_submit|homeworkId|提交内容+成绩+评语|需要|

### 3.4.1. 作业列表

接口地址：`GET /api/v1/user/homework/list?page=1&size=10&courseId=`

功能描述：查看已通过报名课程对应的所有作业，标注是否已提交

请求头：携带登录Token

请求参数（Query参数）

| 参数名   | 类型   | 必填 | 说明             |
| -------- | ------ | ---- | ---------------- |
| page     | int    | 否   | 页码             |
| size     | int    | 否   | 每页条数         |
| courseId | bigint | 否   | 筛选指定课程作业 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 3,
    "list": [
      {
        "homework_id": 1,
        "course_id": 1,
        "course_name": "嵌入式系统实训",
        "homework_title": "第一章：LED点亮代码",
        "homework_content": "完成LED点亮...",
        "deadline": "2026-08-10 23:59:59",
        "submitted": false,
        "create_time": "2026-08-01 14:30:00"
      }
    ]
  }
}
```

### 3.4.2. 作业详情

接口地址：`GET /api/v1/user/homework/detail/{homeworkId}`

功能描述：查看作业完整要求

请求头：携带登录Token

### 3.4.3. 提交作业

接口地址：`POST /api/v1/user/homework/submit`

功能描述：提交作业文字内容与附件文件

请求头：携带登录Token

请求参数（Form表单格式，含文件上传）

| 参数名     | 类型   | 必填 | 说明                     |
| ---------- | ------ | ---- | ------------------------ |
| homeworkId | bigint | 是   | 作业ID                   |
| content    | string | 否   | 文字作答内容             |
| file       | file   | 否   | 作业附件文件，支持多文件 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "作业提交成功",
  "data": {
    "submit_id": 1,
    "submit_time": "2026-08-03 15:30:22"
  }
}
```

### 3.4.4. 我的提交记录

接口地址：`GET /api/v1/user/homework/my_submit?homeworkId=`

功能描述：查看某个作业的提交记录和批改结果

请求头：携带登录Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "submit_id": 1,
    "homework_id": 1,
    "homework_title": "LED点亮代码",
    "course_name": "嵌入式系统实训",
    "submit_content": "已完成LED点亮...",
    "submit_file": "homework_files/led_code.zip",
    "submit_time": "2026-08-03 15:00:00",
    "score": 95,
    "remark": "完成得很好，代码规范"
  }
}
```

## 3.5.学习表现 /api/v1/user/performance

|接口名称|请求方式|地址|参数|返回 data|
|---|---|---|---|---|
|我的表现|GET|/|?courseId=|各课程成绩汇总|

### 3.5.1. 查询个人学习表现

接口地址：`GET /api/v1/user/performance?courseId=`

功能描述：查看个人在各课程中的作业成绩汇总

请求头：携带登录Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "summary": {
      "total_courses": 2,
      "avg_score": 91.5
    },
    "list": [
      {
        "course_id": 1,
        "course_name": "嵌入式系统实训",
        "homework_count": 5,
        "submitted_count": 5,
        "avg_score": 93.5,
        "scores": [95, 90, 92, 98, 93]
      }
    ]
  }
}
```

# 4.管理员后台接口文档--wjc

## 4.1.管理员认证 /api/v1/admin/auth

|接口|Method|地址|参数|返回|鉴权|
|---|---|---|---|---|---|
|管理员登录|POST|/login|body:adminName,password|accessToken、管理员信息|无|
|管理员登出|POST|/logout|header:token|成功提示|需|
|获取管理员信息|GET|/info|header:token|管理员资料|需|
|修改密码|POST|/update\_pwd|body:oldPwd,newPwd|成功|需|
|发送验证码|POST|/send\_code|body:email|发送结果|无|
|重置密码|POST|/reset\_pwd|body:email,code,newPwd|重置完成|无|

### 4.1.1. 管理员登录

接口地址：`POST /api/v1/admin/auth/login`

功能描述：后台管理员账号登录，下发登录令牌

无需鉴权

请求参数（Body-JSON）

| 参数名    | 类型   | 必填 | 说明       |
| --------- | ------ | ---- | ---------- |
| adminName | string | 是   | 管理员账号 |
| password  | string | 是   | 登录密码   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "accessToken": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "adminInfo": {
      "admin_id": 1,
      "admin_name": "admin",
      "real_name": "系统管理员",
      "phone": "13900139000"
    }
  }
}
```

### 4.1.2. 管理员登出

接口地址：`POST /api/v1/admin/auth/logout`

功能描述：销毁管理员登录凭证

请求头：管理员Token

请求参数：无

响应成功示例

```JSON
{
  "code": 200,
  "msg": "后台登出成功",
  "data": null
}
```

### 4.1.3. 获取管理员信息

接口地址：`GET /api/v1/admin/auth/info`
功能描述：获取当前登录管理员个人信息

请求头：管理员Token

请求参数：无

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "admin_id": 1,
    "admin_name": "admin",
    "real_name": "系统管理员",
    "phone": "13900139000",
    "email": "admin@lab.com"
  }
}
```

### 4.1.4. 修改密码

接口地址：`POST /api/v1/admin/auth/update_pwd`

功能描述：管理员修改自身登录密码

请求头：管理员Token

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明   |
| ------ | ------ | ---- | ------ |
| oldPwd | string | 是   | 旧密码 |
| newPwd | string | 是   | 新密码 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "管理员密码修改完成",
  "data": null
}
```

### 4.1.5. 发送验证码（管理员找回密码）

接口地址：`POST /api/v1/admin/auth/send_code`

功能描述：向管理员绑定邮箱发送验证码

无需鉴权

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明             |
| ------ | ------ | ---- | ---------------- |
| email  | string | 是   | 管理员绑定邮箱   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "验证码已下发",
  "data": null
}
```

### 4.1.6. 管理员重置密码

接口地址：`POST /api/v1/admin/auth/reset_pwd`

功能描述：填写邮箱+验证码，重置密码

无需鉴权

请求参数（Body-JSON）

| 参数名 | 类型   | 必填 | 说明       |
| ------ | ------ | ---- | ---------- |
| email  | string | 是   | 邮箱       |
| code   | string | 是   | 邮件验证码 |
| newPwd | string | 是   | 新登录密码 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "管理员密码重置成功",
  "data": null
}
```

## 4.2.课程管理 /api/v1/admin/course

|接口|Method|地址|参数|说明|
|---|---|---|---|---|
|创建课程|POST|/create|body:courseName,courseDesc,coverImg,startTime,endTime,maxSign|创建|
|编辑课程|PUT|/update/{courseId}|body:以上全部+status|修改|
|删除课程|DELETE|/delete/{courseId}|路径参数|删除|
|课程列表|GET|/list|?page=&size=&courseName=&status=|分页搜索|
|课程详情|GET|/detail/{courseId}|路径参数|完整信息|
|上下架|PUT|/status/{courseId}|body:{status}|切换状态|

### 4.2.1. 创建课程

接口地址：`POST /api/v1/admin/course/create`

请求头：管理员Token

请求参数（Body-JSON）

| 参数名     | 类型     | 必填 | 说明             |
| ---------- | -------- | ---- | ---------------- |
| courseName | string   | 是   | 课程名称         |
| courseDesc | string   | 否   | 课程介绍         |
| coverImg   | string   | 否   | 封面图地址       |
| startTime  | datetime | 否   | 开课时间         |
| endTime    | datetime | 否   | 结课时间         |
| maxSign    | int      | 否   | 最大报名人数     |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "课程创建成功",
  "data": {
    "course_id": 1,
    "course_name": "嵌入式系统实训"
  }
}
```

### 4.2.2. 编辑课程

接口地址：`PUT /api/v1/admin/course/update/{courseId}`

请求头：管理员Token

Body入参（全部可选）

| 参数名     | 类型     | 必填 | 说明            |
| ---------- | -------- | ---- | --------------- |
| courseName | string   | 否   | 课程名称        |
| courseDesc | string   | 否   | 课程介绍        |
| coverImg   | string   | 否   | 封面图          |
| startTime  | datetime | 否   | 开课时间        |
| endTime    | datetime | 否   | 结课时间        |
| maxSign    | int      | 否   | 最大报名人数    |
| status     | int      | 否   | 0下架 1上架     |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "课程更新成功",
  "data": {
    "course_id": 1,
    "course_name": "嵌入式系统实训（修订版）"
  }
}
```

### 4.2.3. 删除课程

接口地址：`DELETE /api/v1/admin/course/delete/{courseId}`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "课程删除成功",
  "data": null
}
```

### 4.2.4. 课程列表

接口地址：`GET /api/v1/admin/course/list?page=1&size=10&courseName=&status=`

请求头：管理员Token

Query参数

| 参数名     | 类型   | 必填 | 说明                      |
| ---------- | ------ | ---- | ------------------------- |
| page       | int    | 否   | 页码                      |
| size       | int    | 否   | 每页条数                  |
| courseName | string | 否   | 课程名称模糊搜索          |
| status     | int    | 否   | 0下架 1上架，不传查全部   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 12,
    "list": [
      {
        "course_id": 1,
        "course_name": "嵌入式系统实训",
        "cover_img": "https://xxx/course1.jpg",
        "start_time": "2026-08-01 09:00:00",
        "end_time": "2026-09-30 17:00:00",
        "max_sign": 50,
        "status": 1,
        "sign_count": 25,
        "create_time": "2026-07-20 09:00:00"
      }
    ]
  }
}
```

### 4.2.5. 课程详情

接口地址：`GET /api/v1/admin/course/detail/{courseId}`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "course_id": 1,
    "course_name": "嵌入式系统实训",
    "course_desc": "讲解单片机硬件开发基础与项目实战",
    "cover_img": "https://xxx/course1.jpg",
    "start_time": "2026-08-01 09:00:00",
    "end_time": "2026-09-30 17:00:00",
    "max_sign": 50,
    "status": 1,
    "sign_count": 25,
    "homework_count": 5,
    "create_time": "2026-07-20 09:00:00"
  }
}
```

### 4.2.6. 上下架课程

接口地址：`PUT /api/v1/admin/course/status/{courseId}`

请求头：管理员Token

Body参数

| 参数名 | 类型 | 必填 | 说明           |
| ------ | ---- | ---- | -------------- |
| status | int  | 是   | 0下架 1上架    |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "课程状态已更新",
  "data": {
    "course_id": 1,
    "status": 0,
    "status_text": "已下架"
  }
}
```

## 4.3.作业布置 /api/v1/admin/homework

|接口|Method|地址|参数|说明|
|---|---|---|---|---|
|布置作业|POST|/create|body:courseId,homeworkTitle,homeworkContent,deadline|创建|
|编辑作业|PUT|/update/{homeworkId}|body:homeworkTitle,homeworkContent,deadline|修改|
|删除作业|DELETE|/delete/{homeworkId}|路径参数|删除|
|作业列表|GET|/list|?page=&size=&courseId=|分页筛选|

### 4.3.1. 布置作业

接口地址：`POST /api/v1/admin/homework/create`

请求头：管理员Token

请求参数（Body-JSON）

| 参数名          | 类型     | 必填 | 说明         |
| --------------- | -------- | ---- | ------------ |
| courseId        | bigint   | 是   | 所属课程ID   |
| homeworkTitle   | string   | 是   | 作业标题     |
| homeworkContent | string   | 否   | 作业要求     |
| deadline        | datetime | 否   | 截止提交时间 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "作业布置成功",
  "data": {
    "homework_id": 1,
    "homework_title": "第一章：LED点亮代码"
  }
}
```

### 4.3.2. 编辑作业

接口地址：`PUT /api/v1/admin/homework/update/{homeworkId}`

请求头：管理员Token

Body入参（全部可选）

| 参数名          | 类型     | 必填 | 说明     |
| --------------- | -------- | ---- | -------- |
| homeworkTitle   | string   | 否   | 作业标题 |
| homeworkContent | string   | 否   | 作业要求 |
| deadline        | datetime | 否   | 截止时间 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "作业更新成功",
  "data": {
    "homework_id": 1,
    "homework_title": "第一章：LED点亮代码（修订版）"
  }
}
```

### 4.3.3. 删除作业

接口地址：`DELETE /api/v1/admin/homework/delete/{homeworkId}`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "作业删除成功",
  "data": null
}
```

### 4.3.4. 作业列表

接口地址：`GET /api/v1/admin/homework/list?page=1&size=10&courseId=`

请求头：管理员Token

Query参数

| 参数名   | 类型   | 必填 | 说明         |
| -------- | ------ | ---- | ------------ |
| page     | int    | 否   | 页码         |
| size     | int    | 否   | 每页条数     |
| courseId | bigint | 否   | 按课程筛选   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 5,
    "list": [
      {
        "homework_id": 1,
        "course_id": 1,
        "course_name": "嵌入式系统实训",
        "homework_title": "第一章：LED点亮代码",
        "deadline": "2026-08-10 23:59:59",
        "submit_count": 20,
        "create_time": "2026-08-01 14:30:00"
      }
    ]
  }
}
```

## 4.4.作业批改 /api/v1/admin/homework/submit

|接口|Method|地址|参数|说明|
|---|---|---|---|---|
|提交列表|GET|/list|?page=&size=&homeworkId=&courseId=|分页筛选|
|提交详情|GET|/detail/{submitId}|路径参数|内容+附件|
|评分|PUT|/score/{submitId}|body:score,remark|打分+评语|

### 4.4.1. 学员提交列表

接口地址：`GET /api/v1/admin/homework/submit/list?page=1&size=10&homeworkId=&courseId=`

请求头：管理员Token

Query参数

| 参数名     | 类型   | 必填 | 说明       |
| ---------- | ------ | ---- | ---------- |
| page       | int    | 否   | 页码       |
| size       | int    | 否   | 每页条数   |
| homeworkId | bigint | 否   | 按作业筛选 |
| courseId   | bigint | 否   | 按课程筛选 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 25,
    "list": [
      {
        "submit_id": 1,
        "user_id": 1001,
        "real_name": "张三",
        "homework_id": 1,
        "homework_title": "LED点亮代码",
        "course_name": "嵌入式系统实训",
        "submit_time": "2026-08-03 15:00:00",
        "score": null,
        "status_text": "待批改"
      }
    ]
  }
}
```

### 4.4.2. 提交详情

接口地址：`GET /api/v1/admin/homework/submit/detail/{submitId}`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "submit_id": 1,
    "user_real_name": "张三",
    "user_phone": "13800138001",
    "homework_title": "LED点亮代码",
    "course_name": "嵌入式系统实训",
    "submit_content": "已完成LED点亮，代码见附件",
    "submit_file": "homework_files/led_code.zip",
    "submit_time": "2026-08-03 15:00:00",
    "score": null,
    "remark": null
  }
}
```

### 4.4.3. 评分

接口地址：`PUT /api/v1/admin/homework/submit/score/{submitId}`

请求头：管理员Token

Body参数

| 参数名 | 类型   | 必填 | 说明            |
| ------ | ------ | ---- | --------------- |
| score  | int    | 是   | 分数（0-100）   |
| remark | string | 否   | 教师评语        |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "评分成功",
  "data": {
    "submit_id": 1,
    "score": 95,
    "remark": "完成得很好，代码规范"
  }
}
```

## 4.5.报名管理 /api/v1/admin/sign

|接口|Method|地址|参数|说明|
|---|---|---|---|---|
|报名列表|GET|/list|?page=&size=&courseId=|分页筛选|
|报名详情|GET|/detail/{signId}|路径参数|详细信息|
|取消报名|PUT|/cancel/{signId}|路径参数|管理员取消某条报名|
|导出报名表|GET|/export|?courseId=|导出Excel|

### 4.5.1. 报名列表

接口地址：`GET /api/v1/admin/sign/list?page=1&size=10&courseId=`

请求头：管理员Token

Query参数

| 参数名   | 类型   | 必填 | 说明       |
| -------- | ------ | ---- | ---------- |
| page     | int    | 否   | 页码       |
| size     | int    | 否   | 每页条数   |
| courseId | bigint | 否   | 按课程筛选 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 25,
    "list": [
      {
        "sign_id": 1,
        "user_id": 1001,
        "real_name": "张三",
        "student_id": "202301001",
        "college": "计算机与软件学院",
        "major": "计算机科学",
        "course_id": 1,
        "course_name": "嵌入式系统实训",
        "sign_info": "本人有C语言基础",
        "status": 1,
        "sign_time": "2026-07-24 10:20:33"
      }
    ]
  }
}
```

### 4.5.2. 报名详情

接口地址：`GET /api/v1/admin/sign/detail/{signId}`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "sign_id": 1,
    "user_real_name": "张三",
    "user_phone": "13800138001",
    "user_email": "zhangsan@test.com",
    "student_id": "202301001",
    "college": "计算机与软件学院",
    "major": "计算机科学",
    "course_name": "嵌入式系统实训",
    "sign_info": "本人有C语言基础，希望参与本次实训",
    "status": 1,
    "sign_time": "2026-07-24 10:20:33"
  }
}
```

### 4.5.3. 取消报名

接口地址：`PUT /api/v1/admin/sign/cancel/{signId}`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "报名已取消",
  "data": {
    "sign_id": 1,
    "status": 0
  }
}
```

### 4.5.4. 导出报名表

接口地址：`GET /api/v1/admin/sign/export?courseId=`

请求头：管理员Token

功能描述：导出指定课程的报名数据为 Excel，包含姓名、学号、学院、专业、手机号、报名信息等

响应：直接返回 Excel 文件流下载

## 4.6.报名开关 /api/v1/admin/sign_switch

|接口|Method|地址|参数|说明|
|---|---|---|---|---|
|查询开关|GET|/get|无|返回0/1|
|修改开关|PUT|/update|body:{value:0/1}|切换|
|前台查询|GET|/front/get|无|公开接口|

### 4.6.1. 查询报名开关

接口地址：`GET /api/v1/admin/sign_switch/get`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "value": 1,
    "text": "报名功能已开启"
  }
}
```

### 4.6.2. 修改报名开关

接口地址：`PUT /api/v1/admin/sign_switch/update`

请求头：管理员Token

Body参数

| 参数名 | 类型 | 必填 | 说明                |
| ------ | ---- | ---- | ------------------- |
| value  | int  | 是   | 0关闭报名 1开启报名 |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "报名状态修改成功",
  "data": {
    "value": 0,
    "text": "报名功能已关闭"
  }
}
```

### 4.6.3. 前台查询报名开关

接口地址：`GET /api/v1/admin/sign_switch/front/get`

无需鉴权

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "value": 1,
    "tips": "当前培训报名开放中"
  }
}
```

## 4.7.学员账号管理 /api/v1/admin/user

|接口|Method|地址|参数|说明|
|---|---|---|---|---|
|学员列表|GET|/list|?page=&size=&keyword=&status=|分页搜索|
|学员详情|GET|/detail/{userId}|路径参数|资料+报名+成绩|
|启用/禁用|PUT|/status/{userId}|body:{status:0/1}|切换状态|
|创建账号|POST|/create|body:username,realName,phone,email,grade,major,college,studentId|新增|

### 4.7.1. 学员列表

接口地址：`GET /api/v1/admin/user/list?page=1&size=10&keyword=&status=`

请求头：管理员Token

Query参数

| 参数名  | 类型   | 必填 | 说明                      |
| ------- | ------ | ---- | ------------------------- |
| page    | int    | 否   | 页码                      |
| size    | int    | 否   | 每页条数                  |
| keyword | string | 否   | 姓名/学号模糊搜索         |
| status  | int    | 否   | 0禁用 1正常，不传查全部   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 100,
    "list": [
      {
        "user_id": 1001,
        "username": "stu001",
        "real_name": "张三",
        "student_id": "202301001",
        "college": "计算机与软件学院",
        "major": "计算机科学",
        "grade": "2023级",
        "phone": "138****8001",
        "email": "zhangsan@test.com",
        "status": 1,
        "create_time": "2026-07-01 10:00:00"
      }
    ]
  }
}
```

### 4.7.2. 学员详情

接口地址：`GET /api/v1/admin/user/detail/{userId}`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "user_id": 1001,
    "username": "stu001",
    "real_name": "张三",
    "student_id": "202301001",
    "college": "计算机与软件学院",
    "major": "计算机科学",
    "grade": "2023级",
    "phone": "13800138001",
    "email": "zhangsan@test.com",
    "avatar": "https://xxx/avatar.jpg",
    "status": 1,
    "sign_list": [
      {
        "sign_id": 1,
        "course_name": "嵌入式系统实训",
        "audit_status": 1,
        "status_text": "已通过",
        "sign_time": "2026-07-24 10:00:00"
      }
    ],
    "homework_scores": [
      {
        "homework_title": "LED点亮代码",
        "course_name": "嵌入式系统实训",
        "submit_time": "2026-08-03 15:00:00",
        "score": 95,
        "remark": "完成得很好"
      }
    ],
    "create_time": "2026-07-01 10:00:00"
  }
}
```

### 4.7.3. 启用/禁用学员

接口地址：`PUT /api/v1/admin/user/status/{userId}`

请求头：管理员Token

Body参数

| 参数名 | 类型 | 必填 | 说明          |
| ------ | ---- | ---- | ------------- |
| status | int  | 是   | 0禁用 1启用   |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "账号状态已更新",
  "data": {
    "user_id": 1001,
    "status": 0,
    "status_text": "已禁用"
  }
}
```

### 4.7.4. 创建学员账号

接口地址：`POST /api/v1/admin/user/create`

请求头：管理员Token

请求参数（Body-JSON）

| 参数名    | 类型   | 必填 | 说明         |
| --------- | ------ | ---- | ------------ |
| username  | string | 是   | 登录账号     |
| realName  | string | 是   | 真实姓名     |
| phone     | string | 否   | 手机号       |
| email     | string | 否   | 邮箱         |
| grade     | string | 否   | 年级         |
| major     | string | 否   | 专业         |
| college   | string | 否   | 学院         |
| studentId | string | 否   | 学号         |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "学员账号创建成功，默认密码为 Pass@123",
  "data": {
    "user_id": 1004,
    "username": "stu004",
    "real_name": "赵六"
  }
}
```

## 4.8.学员表现 /api/v1/admin/performance

|接口|Method|地址|参数|说明|
|---|---|---|---|---|
|表现汇总|GET|/list|?page=&size=&courseId=&keyword=|分页筛选|
|学员详细表现|GET|/detail/{userId}|?courseId=|单学员各维度|

### 4.8.1. 表现汇总

接口地址：`GET /api/v1/admin/performance/list?page=1&size=10&courseId=&keyword=`

请求头：管理员Token

Query参数

| 参数名   | 类型   | 必填 | 说明              |
| -------- | ------ | ---- | ----------------- |
| page     | int    | 否   | 页码              |
| size     | int    | 否   | 每页条数          |
| courseId | bigint | 否   | 按课程筛选        |
| keyword  | string | 否   | 姓名/学号搜索     |

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "total": 30,
    "list": [
      {
        "user_id": 1001,
        "real_name": "张三",
        "student_id": "202301001",
        "course_name": "嵌入式系统实训",
        "homework_count": 5,
        "submit_count": 5,
        "avg_score": 93.5,
        "submit_rate": "100%"
      }
    ]
  }
}
```

### 4.8.2. 学员详细表现

接口地址：`GET /api/v1/admin/performance/detail/{userId}?courseId=`

请求头：管理员Token

响应成功示例

```JSON
{
  "code": 200,
  "msg": "成功",
  "data": {
    "user_id": 1001,
    "real_name": "张三",
    "student_id": "202301001",
    "homework_list": [
      {
        "homework_id": 1,
        "homework_title": "LED点亮代码",
        "course_name": "嵌入式系统实训",
        "deadline": "2026-08-10 23:59:59",
        "submit_time": "2026-08-03 15:00:00",
        "score": 95,
        "remark": "完成得很好",
        "on_time": true
      }
    ],
    "avg_score": 93.5,
    "submit_rate": "100%",
    "on_time_rate": "100%"
  }
}
```
